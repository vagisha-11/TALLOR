# Progress bar (usually pre-installed on Kaggle)
tqdm>=4.67.1

# PyTorch stack (check Kaggle's current version first)
torch>=2.0.1
torchvision>=0.15.2
torchaudio>=2.0.2

# Upgrade Snorkel for PyTorch 2.0+ compatibility
snorkel>=0.10.0

# SciPy (compatible with newer PyTorch)
scipy>=1.10.0

# Transformers (good version choice)
transformers>=4.30.0

# scikit-learn (let pip resolve)
scikit-learn

pyarrow==21.0.0​

pydantic==2.11.10
