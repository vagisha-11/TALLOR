# Progress bar (usually pre-installed on Kaggle)
tqdm>=4.67.1

# PyTorch stack (Kaggle already ships 2.6.0+cu124 at time of writing)
torch>=2.0.1
torchvision>=0.15.2
torchaudio>=2.0.2

# Snorkel compatible with PyTorch 2.x
snorkel>=0.10.0

# SciPy compatible with newer PyTorch
scipy>=1.10.0

# Transformers (compatible with modern tokenizers/torch)
transformers>=4.30.0

# Let pip choose latest stable
scikit-learn

# Pins to avoid Kaggle base image conflicts
pyarrow==22.0.0   # Newer required by datasets; ignore RAPIDS pyarrow<20 warnings if not using RAPIDS[web:82][web:84]
pydantic==2.11.10 # Gradio compatibility prefers <2.12 to avoid known breakages[web:93][web:99]
