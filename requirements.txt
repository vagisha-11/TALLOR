# progress bar utility
tqdm>=4.67.1

# compatible PyTorch version (1.7.1 too old for Python 3.11)
torch>=2.0.1
torchvision>=0.15.2
torchaudio>=2.0.2

# snorkel still works with newer torch/scipy
snorkel==0.9.7

# compatible SciPy version
scipy>=1.10.0

# updated Transformers for newer torch
transformers>=4.30.0

# scikit-learn (let pip choose latest stable)
scikit-learn
